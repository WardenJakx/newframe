.nativeControls
  position absolute 
  top 0
  left 0
  right 0
  height 32px
  z-index 20000000000

  .macGrab
    position absolute 
    top 0
    left 0
    bottom 0
    right 0
    -webkit-app-region drag

  .macControls
    position absolute 
    top 0
    left 0
    bottom 0
    width 74px

  .linuxGrab
    position absolute 
    top 0
    right 104px
    bottom 0
    left 0
    -webkit-app-region drag

  .linuxControls
    position absolute 
    top 0px
    right 8px
    bottom 0
    display flex

    .linuxControlsButton
      width 21px
      height 21px
      margin 6px 0px 8px 13px
      display flex
      justify-content center
      align-items center
      color var(--color-text-primary)
      border-radius 11px
      background var(--color-border-subtle)
      flex 1
      flex-shrink 0

      .linuxControlsClose
        height 13px
        width 13px

      .linuxControlsMax
        height 13px
        width 13px

      .linuxControlsMin
        margin-top 5px
        height 13px
        width 13px

      svg
        position relative
        fill currentColor
        transform scale(0.7)
        
      *
        pointer-events none

    .linuxControlsButton:hover
      background var(--color-border-default)

  .windowsGrab
    position absolute 
    top 0
    left 0px
    bottom 0
    right 136px
    -webkit-app-region drag

  .windowsControls
    position absolute 
    top 1px
    right 1px
    bottom 0
    display flex

    .windowsControlsButton
      width 45px
      height 32px
      display flex
      justify-content center
      align-items center
      color var(--color-text-primary)
      border-radius 4px

      svg
        position relative
        fill currentColor
        
      *
        pointer-events none

    .windowsControlsButton:hover
      background var(--color-border-subtle)

.macTitle
  height 30px
  position absolute
  top 0
  left 0
  padding 0px 5px 0px 5px
  z-index 50000
  display flex
  justify-content center
  align-items center

  &:hover
    .titleClose
      background var(--color-system-native-close)

    .titleMin
      background var(--color-system-native-minimize)

    .titleFull
      background var(--color-system-native-maximize)

  .macTitleButton
    height 30px
    width 30px
    position relative
    display flex
    justify-content center
    align-items center

  .titleClose
    width 12px
    height 12px
    background var(--color-shadow-subtle)
    border-radius 50%
    pointer-events none

  .titleMin
    width 12px
    height 12px
    background var(--color-shadow-subtle)
    border-radius 50%
    pointer-events none

  .titleFull
    width 12px
    height 12px
    background var(--color-shadow-subtle)
    border-radius 50%
    pointer-events none
